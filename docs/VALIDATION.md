# Validation

Validated on Linux x86_64 with Rust 1.98.0, eframe 0.36.1, and an X11 desktop on 2026-09-06.

- 114 tests pass: Linux counter accounting, CPU frequency statistics, missing inputs and resets, timestamped history, persistent settings, chart scales and keyboard history access, process parsing and PID reuse/exit races, sorting/filtering and keyboard navigation, and WCAG contrast pairings. Review regressions cover collection during hidden-window logic ticks, pause/resume with an in-flight sample, retained disk/interface selection, unavailable network status, sensor faults and recovery, conversion-dependent hwmon channels, keyboard zoom and saved scale consistency, PECI control targets, scrolling to focused controls and charts, process focus during resizing, and trailing empty command arguments at truncation boundaries.
- `cargo fmt --check` and Clippy across all targets/features pass without warnings.
- An optimized release executable builds successfully.
- Native GUI checks exercise keyboard navigation across all seven pages and inspect the actual AccessKit tree. Current values, associated control labels, active navigation, historical time-cursor values, process rows, and sort state are exposed.
- Live refresh checks collect five samples over 2.2 seconds at a 0.5-second interval. At a 5-second interval, no sample arrives after two seconds and one arrives after the interval. Pause holds the sample count; resume resets history.
- Interface scaling and refresh preferences persist in an isolated test configuration.
- Visual checks cover 1440×1000, 640×480, and a 640×480 window at 200% scale. Compact navigation and page scrolling keep content reachable; settings scroll within the window.
- Live collection was exercised on a 24-thread AMD system with physical RAM, swap, disk counters, network interfaces, and ten temperature sensors. A restricted container view separately verified unavailable network handling.
- Process collection was checked against a bounded CPU-busy child with allocated memory: approximately 100% of one core, expected name/user and resident memory, and removal after exit. The native process page displayed approximately 520 host processes.
- Process GUI checks cover PID sorting in both directions, memory/CPU sort selection, command filtering and empty results, selected process details, and keyboard movement to the first/last row and by page. At 640×480 and 200% scale, page scrolling reaches the table and its process rows remain keyboard-operable; horizontal scrolling exposes the remaining columns.

After the code-review fixes, the full test suite, formatting, Clippy across all targets/features, and optimized release build were rerun successfully. The new regressions use filesystem fixtures, deterministic collector-thread synchronization, and headless egui logic/layout checks. The native desktop checks listed above preceded these fixes and were not repeated for this follow-up.

An earlier whole-codebase review added eight regression tests. They cover replacement network interfaces reusing a name, missing or invalid interface identities and rate recovery, legacy hwmon driver names, per-device AMD Tdie preference with Tctl fallback and CCD readings, distinct accessible chart names and historical values, and keyboard focus visibility through nested process scroll areas at 640×480 and 320×240 logical sizes. The chart-name and process-scroll regressions were confirmed to fail before their fixes. Live metric collection was also rerun successfully in the restricted environment; native screen-reader and Wayland testing remain unperformed.

The follow-up review at `fe82301` resolved five further findings and added eleven regression tests. Disk tests cover same-name replacement, unavailable generations, recovery, and a synchronized replacement during the counter read. Thermal fixtures cover missing package readings, independent sensors with matching driver names, canonical aliases and fault recovery, and VT1211 conversion-dependent channels in current and legacy layouts. Known faulted, disabled, unconverted, and PECI control inputs cannot be revived through an alias. Chart tests retain the selected observation through startup, history trimming, interval changes and gaps, and check expiration and reset. Full-page headless layouts verify that Network uptime remains readable at 320×240, 640×480, 1000×800 and 1440×1000 logical sizes. The disk, chart and header regression tests were confirmed to fail before their fixes.

Live collection after these changes reported valid CPU and disk rates, 24 cores, one whole disk and eleven temperature sensors. The restricted environment exposed no non-loopback network interfaces and correctly displayed its availability notice. Disk rates now require a readable `diskseq`; lifetime counters remain available when that identity is missing. Thermal readings with ambiguous identities may both remain visible. Physical hotplug, VT1211 hardware, and native desktop or screen-reader behavior were not retested; these fixes use filesystem fixtures and headless egui checks.

The review of `58d94f4` resolved three further findings and added five regression tests. Native NVMe multipath fixtures verify that hidden controller paths are excluded while their visible namespace and ordinary NVMe disks remain, with read/write totals and rates counted once. Missing visibility attributes retain compatibility; unreadable or invalid attributes produce notices and omit the uncertain device. Chart tests preserve independent cursors, AccessKit identities, keyboard focus, and arrow navigation when charts move between parents. A full CPU-page test checks resizing between columns, stacked cards, and sidebar-free layouts. Summary layout tests scroll all load-average values into view at 320×240, 640×480, 1000×800, and 1440×1000 logical sizes, checking that two- and three-digit load readings stay intact and within horizontal bounds.

The multipath, chart identity, and compact load regressions were confirmed to fail before their fixes. All 89 tests, formatting, Clippy across all targets/features, and the optimized release build passed after these changes using cached dependencies offline. An independent review of the fixes found no additional issues. Physical multipath hardware and native desktop or screen-reader interaction were not retested; these checks use filesystem fixtures and headless egui/AccessKit integration.

The review of `a068050` resolved two findings and added three regression tests. Filesystem fixtures verify that WM831x battery thermistors and VT8231 external thermistors are omitted with driver-specific notices while calibrated internal channels remain readable. Current and legacy hwmon layouts, canonical aliases, thermal-zone aliases, and systems with no usable temperature inputs are covered. A headless full-app test verifies that Ctrl+plus/equals/minus/0 cannot override the configured 125% scale or desynchronize the Settings selector; interface size is controlled through Settings. All three regressions were confirmed to fail before their fixes. All 92 tests, formatting, Clippy across all targets/features, and the optimized release build pass using cached dependencies offline. Physical WM831x/VT8231 hardware and native desktop or screen-reader interaction were not retested.

The review of `753d5f6` resolved keyboard focus remaining offscreen for ordinary controls in scrollable pages. A new full-app regression tabs through six metric pages after scrolling, and the process regression now checks filters, availability notices, and Clear selection as well as table headers and rows. Both use 320×240 and 640×480 logical viewports and verify that manual scrolling still works after a control gains focus. The regressions were confirmed to fail before the corresponding fixes. All 93 tests, formatting, Clippy across all targets/features, and the optimized release build pass using cached dependencies offline. These are headless egui/AccessKit checks; native desktop and screen-reader interaction were not retested.

The review of `0923360` resolved unavailable network status being displayed as Down. Two filesystem regressions cover missing, invalid, unknown, and unreadable status inputs, administrative flag precedence, definite operational-state fallback, recovery, notices, and continued counter/rate availability. A full-app headless regression checks Up, Down, and Unavailable in both the AccessKit tree and rendered text. All 96 tests, formatting, Clippy across all targets/features, and diff whitespace checks pass using cached dependencies offline. Native desktop and screen-reader interaction were not retested for this change.

The review of `f89c133` resolved two temperature findings and added four regression tests. PECI DIMM sensors remain visible with their labels and thresholds, but cannot supply CPU temperatures, including with CPU-labelled or missing channel labels, legacy hwmon layouts, and thermal-zone sources. IT87 channels explicitly disabled with `tempN_type=0` are omitted through every canonical alias and recover when re-enabled. Missing or unreadable type attributes and other drivers' type-zero readings remain available. The three bug regressions were confirmed to fail before their fixes; the fourth checks preservation of existing CPU driver and channel recognition. All 100 tests, formatting, Clippy across all targets/features, and the optimized release build pass using cached dependencies offline. Physical PECI/IT87 hardware and native desktop or screen-reader interaction were not retested.

The review of `e4873ef` resolved both findings and added three regression tests. CPU voltage-regulator (VRM) readings remain visible with their labels and critical thresholds but cannot supply the CPU headline. Filesystem fixtures cover ASUS WMI channel labels, current and legacy hwmon layouts, VRM-only and mixed CPU/VRM readings, case-insensitive source names, and thermal zones. Process VIRT, RES, and SHR columns now retain complete formatted values and units. A headless table regression inspects painted glyphs for 1023.0 and rounded 1024.0 KiB/MiB/GiB values, including horizontal keyboard access at 320×240, 640×480, and 1440×1000 logical sizes. All three regressions were confirmed to fail before the fixes. All 103 tests, formatting, Clippy across all targets/features, and the optimized release build pass using cached dependencies offline. Independent reviews of both fixes found no additional issues. Physical ASUS hardware and native desktop or screen-reader interaction were not retested.

The review of `b03f59c` resolved combined chipset/CPU temperatures supplying the CPU headline. Two filesystem regressions cover Nuvoton `PCH_CHIP_CPU_MAX_TEMP` in current and legacy hwmon layouts, case-insensitive labels, thermal-zone sources, preserved readings and critical thresholds, and direct CPU readings remaining authoritative. Both regressions failed before the fix. All 105 tests with all features, formatting, Clippy across all targets/features, and the optimized build with all features pass using cached dependencies offline. An independent review found no additional issues. Physical Nuvoton hardware and native desktop or screen-reader interaction were not retested.

The review of `8b96533` resolved numeric process columns truncating complete values. PR, CPU%, and TIME+ now size to the longest formatted value in the matching process list, including offscreen rows. Two headless regressions verify complete rendered glyphs for `-100`, `12800.0`, and `1000:00:00`, horizontal keyboard access at 320×240, 640×480, and 1440×1000 logical sizes, and stable column widths when navigating to initially virtualized rows. The rendering regression was confirmed to fail before the fix. All 107 tests with all features, formatting, Clippy across all targets/features, and diff whitespace checks pass using cached dependencies offline. An independent review found no additional issues. Native desktop and screen-reader interaction were not retested.

The review of `6f7ec3f` resolved both findings and added six regression tests. Raw network-interface names now retain their identity through counter parsing, sysfs lookups, and rate baselines. A filesystem fixture covers non-UTF-8 names, literal backslashes, Unicode replacement characters, Unicode whitespace, Unicode controls versus invalid bytes, independent display names, same-name replacement, removal, and source-read failure/recovery. Process focus tests cover first/middle/last row exits, state-filter changes, simultaneous arrow navigation, PID reuse, empty lists, other controls retaining focus, and manual scrolling. When no process rows remain, Search receives focus and scrolls into view, including at 320×240 and 640×480 logical sizes. A further interaction regression verifies that explicit pointer and AccessKit focus/click actions win over automatic recovery, including when scrolling would otherwise remove the clicked row before rendering. All six regressions were confirmed to fail before the corresponding fixes. All 113 tests with all features, formatting, Clippy across all targets/features, and the optimized build with all features pass using cached dependencies offline. Independent reviews of both fixes found no outstanding issues. These checks use filesystem fixtures and headless egui; native desktop, screen-reader interaction, and live links with unusual names were not exercised.

The review of `5a1eca3` resolved process keyboard focus disappearing when a smaller table viewport stopped rendering the focused row. A full-app headless regression resizes from 1440×1000 to 1440×800, 640×480, and 320×240 logical sizes, checking preserved row identity, vertical visibility, and subsequent arrow navigation. Resize recovery runs only when the viewport changes and respects explicit focus input; existing manual-scrolling and focus-interaction regressions remain covered. All 114 tests with all features, formatting, and Clippy across all targets/features pass using cached dependencies offline. An independent review found no additional issues. Native desktop and screen-reader interaction were not retested.

The inspection feature is opt-in (`cargo build --features inspection`). It is disabled in the normal release build; testing used a loopback-only port. No metrics or history are transmitted by the normal application.

Contrast and programmatic accessibility checks are not a full WCAG conformance assessment. Manual Orca/screen-reader testing and a Wayland session were not available in this validation environment.
